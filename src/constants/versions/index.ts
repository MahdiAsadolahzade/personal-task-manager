// src/constants/versions/index.ts
import { updateNotes as v100 } from "./1.0.0";
import { updateNotes as v101 } from "./1.0.1";

export const allUpdateNotes = [v101, v100]; // make sure order matches
export const CURRENT_UPDATE_NOTES = v100; // must match the CURRENT_APP_VERSION
